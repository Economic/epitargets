% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{tar_parquet_read}
\alias{tar_parquet_read}
\title{Read a Parquet file as a target}
\usage{
tar_parquet_read(name, command, .read_parquet_args, ...)
}
\arguments{
\item{name}{Symbol, name of the target.}

\item{command}{Expression, R code that returns the file path to the Parquet
file.}

\item{.read_parquet_args}{A named list of additional arguments passed to
\code{\link[arrow:read_parquet]{arrow::read_parquet()}}.}

\item{...}{Additional arguments passed to \code{\link[targets:tar_target]{targets::tar_target_raw()}}
for the read target.}
}
\value{
A list of two target objects: a file-tracking target
(\code{name_file}) and a Parquet-reading target (\code{name}).
}
\description{
A convenience wrapper around \code{\link[tarchetypes:tar_file_read]{tarchetypes::tar_file_read()}} for Parquet
files. Creates a pair of targets: one to track the file with
\code{format = "file"}, and another to read the file with \code{\link[arrow:read_parquet]{arrow::read_parquet()}}.
}
\details{
The read target's storage format defaults to \code{"parquet"} (overriding
the usual \code{targets::tar_option_get("format")} inheritance), so the cached
object is written as a Parquet file in \verb{_targets/objects/}. Callers can
override this by passing e.g. \code{format = "rds"} via \code{...}.
}
\examples{
if (identical(Sys.getenv("TAR_EXAMPLES"), "true")) {
targets::tar_dir({
  targets::tar_script({
    library(targets)
    list(
      epitargets::tar_parquet_read(my_data, "data.parquet")
    )
  })
  targets::tar_manifest()
})
}
}
