% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{rstudio_addin_tar_read_stash}
\alias{rstudio_addin_tar_read_stash}
\title{RStudio addin to read a target and stash it}
\usage{
rstudio_addin_tar_read_stash()
}
\value{
The target value, invisibly (also stashed as \code{.target}).
}
\description{
Reads the target named under the cursor (or the active selection) and stashes
it via \code{\link[=tar_read_stash]{tar_read_stash()}}, leaving a copy in the global environment as
\code{.target}. Bind it to a keyboard shortcut in the RStudio addin manager or a
Positron keybinding, analogous to \code{targets::rstudio_addin_tar_read()}.
}
\details{
The symbol under the cursor is extracted with
\code{\link[atcursor:get_word_or_selection]{atcursor::get_word_or_selection()}}, which works in RStudio, VS Code, and
Positron. \code{atcursor} is an optional dependency; if it is not installed the
addin prompts to install it from its r-universe repository. If the cursor
is on whitespace with no selection, the addin reports this and does
nothing.
}
