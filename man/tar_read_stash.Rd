% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{tar_read_stash}
\alias{tar_read_stash}
\title{Read a target and stash it in an environment}
\usage{
tar_read_stash(
  name,
  stash_name = ".target",
  print_value = TRUE,
  envir = globalenv(),
  store = targets::tar_config_get("store"),
  ...
)
}
\arguments{
\item{name}{Symbol or string, the target to read. NSE-friendly, matching
\code{\link[targets:tar_read]{targets::tar_read()}}.}

\item{stash_name}{String, the variable name to assign the value to in
\code{envir}. Defaults to \code{".target"}, a leading-dot scratch slot. If it
already exists in \code{envir} it is silently overwritten.}

\item{print_value}{Logical. If \code{TRUE} (default), the value is printed after
assignment so the addin behaves like the stock \code{tar_read} addin.}

\item{envir}{Environment to stash the value into. Defaults to
\code{\link[=globalenv]{globalenv()}}.}

\item{store}{Character, the targets data store. Passed through to
\code{\link[targets:tar_read_raw]{targets::tar_read_raw()}}.}

\item{...}{Additional arguments passed to \code{\link[targets:tar_read_raw]{targets::tar_read_raw()}}
(e.g. \code{branches}).}
}
\value{
The target value, invisibly.
}
\description{
Reads a target with \code{\link[targets:tar_read]{targets::tar_read()}} and, in addition to returning it,
assigns a copy into \code{envir} under a fixed scratch name (\code{.target} by
default). This lets you keep a handle to the value for interactive
exploration without re-running the read. It is the engine behind the
\code{\link[=rstudio_addin_tar_read_stash]{rstudio_addin_tar_read_stash()}} addin.
}
\details{
A resolved character target name is passed to
\code{\link[targets:tar_read_raw]{targets::tar_read_raw()}} rather than \code{\link[targets:tar_read]{targets::tar_read()}}, because the
latter uses non-standard evaluation and would otherwise look up a target
named after the variable rather than its value.

When called from a non-interactive context (a script or pipeline), a
warning is emitted but the value is still stashed.
}
\examples{
if (identical(Sys.getenv("TAR_EXAMPLES"), "true")) {
targets::tar_dir({
  targets::tar_script({
    library(targets)
    list(
      targets::tar_target(x, 1 + 1)
    )
  })
  targets::tar_make()
  tar_read_stash(x)
  .target
})
}
}
